# Session secret used by iron-session. Set to a long random string in production.
CASHTRACK_SESSION_SECRET=replace-with-a-secure-random-secret

# Directory where CashTrack stores its SQLite database and backup files.
CASHTRACK_DATA_DIR=./data

# Optional override for the LAN sync base URL shown in the UI.
# Defaults to http://<local-ip>:3000 when unset.
SYNC_HOST=
